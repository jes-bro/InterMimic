# GENERATED by scripts/generate_gen2_arms.py -- gen-2 grid cell g2_mlp_ret_stock__f0.
# arch=mlp refs=ret recipe=stock fold=f0 buffer_mult=12 (test=['sub10', 'sub13', 'sub16']).
# Bodies = 13 reals + synthetics beyond the 2.106 leak floor.
params:
  seed: -1

  algo:
    name: intermimic

  model:
    name: intermimic

  network:
    # MLP builder (run.py:212) instead of intermimic_transformer (run.py:217).
    # Pairs with numObs 3230 + no useTransformerObs in
    # omomo_teacher_src2_mlp_retarget_lowbuf.yaml -- the transformer builder
    # requires that env flag, the MLP one must not get it.
    name: intermimic
    separate: True

    space:
      continuous:
        mu_activation: None
        sigma_activation: None
        mu_init:
          name: default
        sigma_init:
          name: const_initializer
          val: -2.9
        fixed_sigma: True
        learn_sigma: False

    mlp:
      units: [1024, 1024, 512]
      activation: relu
      d2rl: False

      initializer:
        name: default
      regularizer:
        name: None

  load_checkpoint: False

  config:
    name: mimic
    # MLP + RETARGET, STOCK recipe (constant LR 2e-5, no normalize_value) -- the
    # same optimizer the src2_aug MLP baseline trained with. One-variable reads:
    #   vs omomo_teacher_src2_mlp_retarget_nvadlr.yaml : recipe only (this file
    #      flips normalize_value False + lr_schedule constant, drops kl_threshold)
    #   vs src2_aug : NOT single-knob -- retargeting, plus the env-side package
    #      inherited from the retarget family (sub121-free body list,
    #      cpuMotionData, lowbuf 12.0). Read "retargeting under the stock recipe"
    #      from this arm, and isolate the recipe via the nvadlr neighbour instead.
    # Distinct name = own checkpoint dir, no clobber.
    full_experiment_name: smplx_teacher_g2_mlp_ret_stock_syn0__f0
    env_name: rlgpu
    multi_gpu: False
    ppo: True
    mixed_precision: False
    normalize_input: True
    normalize_value: False
    reward_shaper:
      scale_value: 1
    normalize_advantage: True
    gamma: 0.99
    tau: 0.95
    learning_rate: 2e-5
    lr_schedule: constant
    score_to_win: 20000
    max_epochs: 100000
    save_best_after: 100
    save_frequency: 100
    print_stats: True
    grad_norm: 1.0
    entropy_coef: 0.0
    truncate_grads: False
    e_clip: 0.2
    horizon_length: 32
    minibatch_size: 16384
    mini_epochs: 6
    critic_coef: 5
    clip_value: False
    seq_len: 4
    bounds_loss_coef: 10
    enable_eps_greedy: False
    save_intermediate: True
    resume_from: 'None'
